<%# By default, the header block for each post on the index/month/year pages is the same as
  # the header block on the show (individual post) page. Override this template to change that.
%>
<%= render partial: 'blog/show/post_header', locals: { post: post } %>
