<% @page_title = @edition.name || t( '.title' ) %>

<section>
  <header>
    <h2>
      <%= @page_title %>
    </h2>
  </header>

  <p>
    <%= @edition.name %> (sent at @send.sent_at.to_s( :time ) on @send.sent_at.to_s( :shinydate )
  </p>

  <%# TODO: HTML rendering of MJML templated newsletter %>
</section>
