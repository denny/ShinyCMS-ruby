<%= t( 'shinycms.upvotes.vote' ) %>:

<% if current_user&.voted_up_on?( resource ) %>
  <%= button_to t( 'shinycms.upvotes.remove_upvote' ),
              shinycms.destroy_vote_path( resource.class.name.underscore, resource.id ), class: 'link-button', method: :delete %>
<% elsif current_user&.voted_down_on?( resource ) %>
  <%= button_to t( 'shinycms.upvotes.remove_downvote' ),
              shinycms.destroy_vote_path( resource.class.name.underscore, resource.id ), class: 'link-button', method: :delete %>
<% else %>
  <%= button_to t( 'shinycms.upvotes.add_upvote' ),
              shinycms.create_vote_path( resource.class.name.underscore, resource.id, 'up' ), class: 'link-button' %>
  /
  <%= button_to t( 'shinycms.upvotes.add_downvote' ),
              shinycms.create_vote_path( resource.class.name.underscore, resource.id, 'down' ), class: 'link-button' %>
<% end %>

(<%= resource.get_upvotes.size %> / <%= resource.get_downvotes.size %>)
