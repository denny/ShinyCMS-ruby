<% @page_title = I18n.t( 'shiny_shop.products.index.title' ) %>

<% if @products.any? %>
  <% @products.each do |product| %>
    <h2><%= product.name %></h2>
    <p>
      <%= number_to_currency product.price.to_d/100, unit: '&pound;'.html_safe %>
    </p>
  <% end %>
<% else %>
  No products found.
<% end %>
