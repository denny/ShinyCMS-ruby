<section>
  <% if @user_profile.profile_pic.present? %>
  <%= image_tag @user_profile.profile_pic.variant( resize_to_limit: [400,400] ).processed.service_url, class: 'user-profile-pic' %>
  <% end %>

  <header>
    <h2>
      <%= @user_profile.name %>
    </h2>
  </header>

  <% if @user_profile.bio.present? %>
  <%= simple_format @user_profile.bio %>
  <% end %>

  <br>

  <p>
    Joined on <%= display_date( @user_profile.created_at ) %>
  </p>

  <% if @user_profile.profile_pic.present? %>
  <div class="clear-both"></div>
  <% end %>
</section>
