<% if current_user.can? :list, :mailing_lists %>

  <%= render_admin_menu_section( t( 'shiny_lists.admin.lists.menu' ), 'list' ) do %>

    <%= render_admin_menu_item(
        t( 'shiny_lists.admin.lists.index.title' ), shiny_lists.lists_path, 'list' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :add, :mailing_lists ),
        t( 'shiny_lists.admin.lists.new.title' ), shiny_lists.new_list_path, 'playlist-add' ) %>

  <% end %>

<% end %>
