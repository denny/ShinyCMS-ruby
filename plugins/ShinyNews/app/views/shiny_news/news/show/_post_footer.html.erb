<footer>
  <% if feature_enabled? :comments %>
    <%= render partial: 'news/show/footer_discussion_links', locals: { post: post } %>
  <% end %>

  <% if feature_enabled? :news_votes %>
    <%= render partial: 'news/show/footer_votable_links', locals: { post: post } %>
  <% end %>

  <% if feature_enabled? :comments %>
    <%= render partial: 'discussions/moderation_links', locals: { discussion: post.discussion.presence } %>
  <% end %>
</footer>
