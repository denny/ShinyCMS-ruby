version: 2

workflows:
  setup_and_test:
    jobs:
      - install_dependencies:
          filters:
            branches:
              ignore: gh-pages
      - rubocop_and_bundle_audit:
          filters:
            branches:
              ignore: gh-pages
      - rspec:
          filters:
            branches:
              ignore: gh-pages
