// TODO: FIXME: need to dynamically generate entry points for each theme's JS
require( 'halcyonic'   )

// Halcyonic theme uses SmartMenus - but currently loads it from /public, not here
//require( 'smartmenus' )
