<% @page_title = @page.title %>
<h1>
  <%= @page.title %>
</h1>
<p>
  <!-- img src="<%#= @page.elements.image %>" -->
  <img src="<%= @page.element('image') %>">
</p>
<h2>
  <%#= @page.elements.title %>
  <%= @page.element('title') %>
</h2>
<p>
  <%#= @page.elements.intro %>
  <%= @page.element('intro') %>
</p>
