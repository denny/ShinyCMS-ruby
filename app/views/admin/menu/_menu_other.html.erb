  <%= render_admin_menu_section( t( 'admin.other.title' ), 'settings' ) do %>

    <% plugins_for_admin_other_menu.each do |plugin| %>
    <%= render partial: "#{plugin.name.underscore}/admin/menu/other_item" %>
    <% end %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :spam_comments ),
        t( 'admin.comments.title' ), comments_path, 'trash' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :email_previews ),
        t( 'admin.emails.title' ), rails_email_preview_path, 'envelope-letter' ) %>

  <% end %>
