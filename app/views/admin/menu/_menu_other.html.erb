  <%= render_admin_menu_section_start( t( 'admin.other.title' ), 'settings' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :shared_content ),
        t( 'admin.shared_content.title' ), shared_content_path, 'list' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :settings ),
        t( 'admin.settings.title' ), settings_path, 'settings' ) %>

  <%= render_admin_menu_section_end %>
