  <%= render_admin_menu_section_start( t( 'admin.other.title' ), 'applications' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :inserts ),
        t( 'admin.inserts.title' ), inserts_path, 'list' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :settings ),
        t( 'admin.site_settings.title' ), admin_site_settings_path, 'settings' ) %>

    <%= render_admin_menu_item_if( current_user.can?( :list, :feature_flags ),
        t( 'admin.feature_flags.title' ), feature_flags_path, 'toggle-off' ) %>

  <%= render_admin_menu_section_end %>
