<% @page_title = 'List templates' %>

<table class="list">
  <% @templates.each do |template| %>
	<tr class="showfocus">
		<td>
			<%= template.name %>
		</td>
		<td class="actions">
			<%= link_to 'Edit', edit_admin_pages_template_path( template ) %>
		</td>
	</tr>
	<% end %>
</table>
