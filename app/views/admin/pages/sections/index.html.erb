<% @page_title = 'List sections' %>

<table class="list">
  <% @tl_sections.each do |section| %>
		<%= render partial: 'section_contents', locals: {
			section: section,
			view_path: '/pages',
			indent: 0
		} %>
	<% end %>
</table>
