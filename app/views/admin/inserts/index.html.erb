<% @page_title = t( '.title' ) %>

<%= form_with model: @insert_set, name: 'insert_set', url: inserts_path, method: :put do |f| %>
<table class="table table-responsive-sm table-striped">
  <%= f.fields_for :elements do |fields| %>
    <%= render partial: 'element', locals: {
      fields: fields,
      content: :content,
      name: fields.object.name,
      content_type: fields.object.content_type
    } %>
  <% end %>
</table>
<p class="top-margin">
  <%= f.submit t( 'update' ) %>
</p>
<% end %>

<%= form_for :insert_element, name: 'new_element', url: create_insert_path, method: :post do |f2| %>
  <fieldset>
    <legend>
      Add new insert
    </legend>
    <p>
      <%= f2.text_field :name, placeholder: t( '.name_placeholder' ), class: 'textshort' %>
      <%= f2.select :content_type, content_types %>
      <br><%= f2.text_field :content, placeholder: t( '.content_placeholder' ), class: 'textlong' %>
    </p>
    <p>
      <%= f2.submit t( 'add' ) %>
    </p>
  </fieldset>
<% end %>
