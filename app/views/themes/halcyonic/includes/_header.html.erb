        <section id="header">
          <div class="container">
            <div class="row">
              <div class="col-12">
                <h1>
                  <a href="/" id="logo"><%= @site_name %></a>
                </h1>
                <nav id="nav">
                  <%= render partial: 'themes/shinycms/includes/menu' %>
                </nav>
              </div>
            </div>
          </div>
          <%= render partial: 'includes/messages' %>
        <% unless @page&.default_page? %>
        </section>
        <% end %>
