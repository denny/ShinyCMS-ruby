<section>
  <%= render partial: 'blogs/post_in_list_header', locals: { post: post } %>

  <%= sanitize post.teaser( paragraphs: 3 ) %>

  <footer>
  <% if post.discussion.present? %>
    <%= link_to t( 'discussions.n_comments', count: post.discussion.visible_comments.count ),
                post.path( anchor: 'comments' ) %>
  <% else %>
    <i>Comments have been disabled on this post</i>
  <% end %>
  </footer>
</section>
