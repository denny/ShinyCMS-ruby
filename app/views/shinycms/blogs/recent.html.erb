<% @page_title = @blog.title || 'Recent blog posts' %>

<% @sidebar_position = 'right' %>

<% @posts.each do |post| %>
  <%= render partial: 'blogs/post_in_list', locals: { post: post } %>
<% end %>

<%= render partial: 'right_sidebar_join' %>
<%= render partial: 'sidebar' %>
