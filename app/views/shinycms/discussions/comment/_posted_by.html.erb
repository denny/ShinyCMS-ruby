Posted by
<% if comment.author.blank? %>
  <%= t( '.anonymous' ) %>
<% elsif comment.authenticated_author? %>
    <%= user_profile_link( comment.author ) %>
<% else %>
  <% if comment.author.website.present? %>
    <%= link_to comment.author.name, comment.author.website %>
  <% else %>
    <%= comment.author.name %>
  <% end %>
<% end %>
at <%= display_time_on_date( comment.posted_at ) %>
